{
  "name": "node-server-with-es6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-minify --target=node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^8.0.1"
  },
  "devDependencies": {
    "parcel": "^1.12.3"
  }
}
